```bash

👤 Автор 
[Sashcash]

📋 Описание 📋 
- Проверка функциональности на сайте Сабу и Тензор - переходы, блок "Сила в людях", сравнение изображений
- Проверка регионов - смена региона с Екатеринбурга на Камчатский край, наличие партнеров
- Проверка версии - скачивание установочного файла и сравнение версии

📁 Структура проекта
├── tensor_tests.py          # Основные тесты
├── utils/
│   ├── checks.py            # Вспомогательные функции
│   └── locators.py          # Локаторы элементов
├── requirements.txt         # Зависимости
├── downloaded_files/        # Папка для скачанных файлов
└── allure_results/          # Результаты тестов 

🚀 Установка 🚀
# Установка всех зависимостей #
pip install -r requirements.txt
# Обновление всех пакетов (если нужно) #
pip install --upgrade -r requirements.txt

🚀 Запуск тестов 🚀
# Запуск тестов по одному, браузер развернут на весь экран, с демонстрацией действий на экране и отчетом (медленно)#
pytest tensor_tests.py --alluredir=allure_results --demo --maximize
# Запуск тестов в три потока с генерацией отчета (быстро) #
pytest tensor_tests.py --alluredir=allure_results -n 3
# Запуск тестов по метке смок (в нашем случае все смок :) #
pytest tensor_tests.py -m smoke

# Открыть отчет после прогона тестов #
allure serve allure_results